global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # เพิ่ม service ที่ต้องการ monitor ที่นี่
  # - job_name: face-recognition-server
  #   static_configs:
  #     - targets: ["infra-server:8080"]
